#include<stdio.h>
int main(void)
{
    int radius = 6;
    float pi = 3.14;
    printf("perimeter of circle = %.5f inches\n" , 2*pi*radius);
    printf("area of circle = %.5f square inches\n", pi*radius*radius);

}