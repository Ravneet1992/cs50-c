#include<stdio.h>
int main(void)
{
    int length = 7;
    int width = 5;

    printf("area of rectangle = %d square inches\n" , length*width);
    printf("perimeter of rectangle = %d inches\n", 2*(length+width));
}